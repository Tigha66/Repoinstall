# Copy to .env and fill in. All optional — the prototype runs without any of these.
PORT=4000
BUSINESS_NAME=Bright Smile Dental
AGENT_NAME=Aria
HOURS=Mon–Sat, 9am–6pm

# Optional: add an OpenAI key for natural free-form conversation (instead of scripted).
# OPENAI_API_KEY=sk-xxxxxxxx
# OPENAI_MODEL=gpt-4o-mini
